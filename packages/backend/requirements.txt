# Python dependencies used by the backend service layer.
# Only the design-system upload extractor shells out to Python today
# (packages/backend/src/services/upload-extractor-py.ts embeds the
# script; `runPythonUploadExtractor` spawns `python script.py`).
# Install with `python -m pip install --user -r requirements.txt`, or
# from inside the app via Settings → "Python for uploads" → Install
# pypdf.
#
# The pin mirrors PYPDF_REQUIRED_VERSION in
# packages/backend/src/services/pypdf-version.ts; older releases carry
# malformed-PDF resource-exhaustion advisories and are refused at runtime.

pypdf==6.18.0
