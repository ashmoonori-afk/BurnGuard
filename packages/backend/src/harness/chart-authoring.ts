export const CHART_AUTHORING_RULES = `## Native data charts
When the user needs a chart, use BurnGuard's original data-driven SVG chart renderer in slides, web pages and graphics. Supported types: area, line, bar, composed, radar, pie, radial, sankey. Do not use image generation, CDN chart libraries or screenshot images for these charts.
Place a figure at the intended position in the page: <figure data-bg-chart="revenue"><script type="application/json" data-bg-chart-config>{"schema_version":1,"id":"revenue","type":"bar","title":"Quarterly revenue","unit":"USD","source":"User-provided figures","categories":["Q1","Q2","Q3"],"series":[{"name":"Revenue","values":[20,35,28]}]}</script></figure>. Escape < as \\u003c inside JSON. The complete JSON is rendered during live preview, and inline SVG plus an accessible data table are saved before publication. No runtime is needed for HTML export. Keep figure placement/size appropriate to the artboard; on decks use generous chart space and readable labels. Never combine independent charts into one bitmap.
ChartV1 exact keys: schema_version:1, id (starts with an ASCII letter, letters/digits/_/- only, max40, unique per document), type, title (max120), optional description (max300), source (max160), unit (max20), width (320–2400, default960), height (300–1600, default540), theme (brand/paper/midnight/mono), colors (up to6 six-digit HEX). Theme brand also accepts CSS variables --bg-chart-1 through --bg-chart-6. Match the selected design system's palette/font; do not default every chart to cream. Preserve existing config IDs when editing. Maximum32 charts per HTML file. No extra keys, callbacks, URLs or executable code.
For area/line/bar/composed/radar/pie/radial add categories (1–36 labels, max60 chars each), series (1–6 objects: name max60, values same length as categories). Each value must be finite within +/-1e12; null is a real gap for area/line/bar/composed, never replace missing data with zero. For composed, each series requires kind:bar/line/area and all series must share a compatible unit and scale. Bar and area baselines include zero. Radar needs >=3 comparable axes, nonnegative values and one common scale. Pie allows one nonnegative series and <=12 categories; use it only for parts of a meaningful whole. Radial allows one nonnegative series, <=8 categories, radial_max (positive target, default100); values cannot exceed the target. Pie/radar/radial do not accept null.
For sankey use nodes and links instead of categories/series/radial_max. nodes:2–16 {id,label} objects (same ID rules, labels max60); links:1–32 {source,target,value} objects referencing node IDs. Positive values 1e-9–1e12, no isolated nodes, duplicate links, self-links or cycles. Link thickness uses one shared flow scale; report imbalances rather than inventing flows. Example nodes:[{id:"in",label:"Visits"},{id:"out",label:"Orders"}], links:[{source:"in",target:"out",value:40}]. Do not use tabs/newlines in series/category/node labels.
Choose line for time trends, area for magnitude over time, bar for comparisons, composed for compatible measures, radar for comparable profiles, pie for composition, radial for progress against an explicit target, and sankey for measured flows. Use only supplied/verified numbers and label source, units and period. If data is missing, ask for it or mark all sample data explicitly as illustrative. Check arithmetic, denominators, missing values and chart/artboard fit before finishing. Quality advice does not gate the user's export choice.`;
