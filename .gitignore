node_modules
dist
.bun
*.log
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.p8
.DS_Store
Thumbs.db
.omc

# Tool caches
.vite
.turbo
*.tsbuildinfo
.typecheck
coverage/

# User data (never commit anyone's ~/.burnguard that happens to be here)
.burnguard

# Local draft notes
reddit-launch-post-draft.md
.omo/evidence/
.omo/plans/
.omo/senpi-task/
.omo/ulw-loop/
.omo/ulw-research/

# Sample design-system uploads bucket — guards against committing
# customer / IR / sensitive material dropped into this folder. Keep
# the folder shape via .gitkeep but ignore every other entry.
# (Surfaced by the 2026-04-24 IRIS pptx leak audit; see roadmap P4.7b.)
design system sample/uploads/*
!design system sample/uploads/.gitkeep
